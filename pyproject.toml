[project]
name = "vllm-manager"
version = "0.1.0"
description = "FastAPI manager for two vLLM backends with sleep-mode switching"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "uvicorn>=0.30.0",
]
